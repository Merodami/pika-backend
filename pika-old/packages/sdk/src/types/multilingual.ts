import type { MultilingualContent } from '@pika/types-core'

/**
 * Re-export for SDK consistency
 * The SDK uses the centralized type from @pika/types
 */
export type MultilingualText = MultilingualContent
